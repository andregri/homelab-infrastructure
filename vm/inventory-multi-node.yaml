all:
  hosts:
    node1:
      ansible_host: "{{ lookup('env', 'NODE1') }}"
      ansible_user: cloud_user
      ansible_ssh_private_key_file: ./key
    node2:
      ansible_host: "{{ lookup('env', 'NODE2') }}"
      ansible_user: cloud_user
      ansible_ssh_private_key_file: ./key
    node3:
      ansible_host: "{{ lookup('env', 'NODE3') }}"
      ansible_user: cloud_user
      ansible_ssh_private_key_file: ./key
  
  children:
    control_plane:
      hosts:
        node1:

    workers:
      hosts:
        node2:
        node3: